# Build output. Regenerated by build.py on every run and by CI on every deploy.
_site/

# Retired files waiting to be deleted by hand. A remote session cannot delete on
# this mount, so anything withdrawn is moved here instead.
_to_delete/

__pycache__/
*.pyc
.DS_Store
.hugo_build.lock
