Markdown==3.7
# Syntax highlighting, done once at build time instead of 121 KB of JavaScript on every post
# page. Pinned like the rest: a highlighter changing its token classes is a silent visual change.
Pygments==2.20.0
Pillow==11.0.0
tzdata==2025.2

# Minification, run by minify.py as the last step before the artifact is
# uploaded. Pinned, because a minifier changing its mind about what is safe to
# remove is a silent visual regression rather than a build failure.
rcssmin==1.2.2
rjsmin==1.2.5
minify-html==0.18.1

# Lossless image recompression, same step. mozjpeg rewrites Huffman tables and
# makes a JPEG progressive; oxipng re-deflates a PNG and drops chunks browsers
# ignore. Neither changes a pixel, which is why they can run unattended.
mozjpeg-lossless-optimization==1.3.2
pyoxipng==9.1.1
